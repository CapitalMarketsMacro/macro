export * from './lib/macro-angular-grid/macro-angular-grid';

// Back-compat: the format types now live in the shared @macro/macro-grid-format lib.
export type { FormatType, ColumnFormatConfig, ColumnFormatMap, ColumnFormatSpec } from '@macro/macro-grid-format';
