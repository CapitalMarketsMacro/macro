# TypeScript 6 is ahead of @angular/build's declared peer range; installs need
# legacy peer resolution until Angular widens it. Keeps `npm ci` / `npm install`
# working identically on dev machines and CI without remembering the flag.
legacy-peer-deps=true
